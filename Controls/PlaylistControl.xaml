<UserControl x:Class="Movie_Cleanup.Controls.PlaylistControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="23">
    <Grid DataContext="{Binding}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"></ColumnDefinition>
            <ColumnDefinition ></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Name="imgPlalistImage" Source="{Binding PlaylistArt}" Height="22" Width="20" VerticalAlignment="Center"
                        />
        <Label Grid.Column="1" x:Name="lblPlaylistName" Content="{Binding PlaylistName}" FontSize="10" VerticalAlignment="Center"></Label>
    </Grid>
</UserControl>
